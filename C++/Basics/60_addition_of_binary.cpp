#include <iostream>
using namespace std;

int main(){

    int arr[10],arr2[10];
    int len,i;
    cout<<"Enter the lenght of array: ";
    cin>>len;
    cout<<"Input the 1st binary number: ";
    for(i=0;i<len;i++){
        cin>>arr[i];
    }
    
    cout<<"Input the 2nd binary number: ";
    for(i=0;i<len;i++){
        cin>>arr2[i];
    }
    
    for(i=0;i<len;i++){
        if(arr[i]==arr2[i]){
            
        }
    }

    
return 0;
}